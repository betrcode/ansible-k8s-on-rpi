---

- name: Set hostname
  hosts: pis
  become: yes

  tasks:
    - name: Set hostname as defined in inventory
      hostname:
        name: "{{ inventory_hostname }}"
    - name: Setup hosts file
      template:
        src: hosts
        dest: /etc/hosts
